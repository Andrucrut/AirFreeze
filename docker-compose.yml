services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: airfreeze
      POSTGRES_PASSWORD: airfreeze
      POSTGRES_DB: airfreeze
    ports:
      - "5433:5432"
    volumes:
      - airfreeze_pgdata:/var/lib/postgresql/data

volumes:
  airfreeze_pgdata:
